# libs

全志 Tina Linux 的手工构建脚本在 [allwinner-tina-build.sh](./libs/allwinner-tina-build.sh)。

脚本会按全志 `mk` 配方从 `libs/lvgl/lv_conf_template.h` 生成专用的 `lv_conf.h`，默认输出到 `libs/lv_conf_allwinner_tina.h`。

详细说明见 [Tina Linux 手工构建说明](./docs/zh/tina-linux-manual-build.md)。
